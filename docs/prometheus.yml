# Prometheus scrape configuration for local docker-compose development
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'demo-app'
    static_configs:
      - targets: ['app:3000']
    metrics_path: '/metrics'
